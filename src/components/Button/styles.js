import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  default: {
    width: 40,
    height: 40,
    backgroundColor: '#fff',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
